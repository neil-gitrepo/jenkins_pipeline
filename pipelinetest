import jenkins.*
import hudson.*
import java.io.File
node { 
    stage('Build') { 
        println("build")
        println(env.WORKSPACE)
        
    }
    stage('Test') {
    def content = new File("${env.WORKSPACE}/README").readLines()
    println(content)
    }
}
