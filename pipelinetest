node { 
    stage('Build') { 
        println("build")
        println("WORKSPACE:"+${WORKSPACE})
        println("ENV:"+${env})
        
    }
    stage('Test') {
        sh 'cat ${env.WORKSPACE}/README'
    }
}
