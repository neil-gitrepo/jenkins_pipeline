import jenkins.*
import hudson.*
node { 
    stage('Build') { 
        println("build")
        println(env.WORKSPACE)
        
    }
    stage('Test') {
        sh 'cat ${env.WORKSPACE}/README'
    }
}
